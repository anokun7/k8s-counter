apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: noop-demo
# namespace: noop
spec:
  gateways:
  - istio-system/ingress-gateway
  hosts:
  - '*'
  http:
  - route:
    - destination:
        host: web
        port:
          number: 2015
